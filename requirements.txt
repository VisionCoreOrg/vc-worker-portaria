# =============================================================================
# requirements.txt — vc-worker-portaria
#
# ATENÇÃO: o pacote `onnxruntime` NÃO está listado aqui.
# Ele é instalado pelo Dockerfile de forma condicional:
#   - CPU (padrão): onnxruntime
#   - GPU NVIDIA:   onnxruntime-gpu  (via --build-arg GPU_BUILD=nvidia)
# =============================================================================

# — OCR
easyocr==1.7.2

# — PyTorch (obrigatório pelo easyocr)
torch==2.10.0
torchvision==0.25.0

# — Visão computacional
opencv-python-headless==4.13.0.92
numpy==2.4.2
pillow==12.1.1
scikit-image==0.26.0
scipy==1.17.1
imageio==2.37.2
tifffile==2026.2.20
lazy_loader==0.4
shapely==2.1.2
pyclipper==1.4.0
python-bidi==0.6.7

# — Storage (MinIO via boto3)
boto3==1.42.58
botocore==1.42.58
s3transfer==0.16.0
jmespath==1.1.0

# — Fila de eventos
redis==5.2.1

# — HTTP / API Core
requests==2.32.5
certifi==2026.1.4
charset-normalizer==3.4.4
idna==3.11
urllib3==2.6.3

# — Configuração
python-dotenv==1.2.1

# — Dependências transitivas (torch / easyocr / boto3)
PyYAML==6.0.3
packaging==26.0
filelock==3.24.3
fsspec==2026.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
networkx==3.6.1
sympy==1.14.0
mpmath==1.3.0
typing_extensions==4.15.0
python-dateutil==2.9.0.post0
six==1.17.0
setuptools==82.0.0
